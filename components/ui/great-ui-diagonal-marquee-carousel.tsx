import Image from "next/image";import type {CardItem} from "@/config/portfolio";export function GreatUiDiagonalMarqueeCarousel({cards}:{cards:CardItem[]}){return <div className="marquee-bg" aria-hidden="true">{[0,1,2,3].map(row=><div className={`marquee-row row-${row}`} key={row}><div className="marquee-track">{[...cards,...cards].map((c,i)=><div className="marquee-card" key={`${row}-${c.id}-${i}`}><Image src={c.url} alt="" fill sizes="320px" draggable={false}/></div>)}</div></div>)}</div>}