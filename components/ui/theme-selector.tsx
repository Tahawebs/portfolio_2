"use client";import {Sun,Moon,Monitor} from "lucide-react";import {motion} from "framer-motion";import {useTheme} from "next-themes";import {useEffect,useState} from "react";const ops=[{v:"light",I:Sun},{v:"dark",I:Moon},{v:"system",I:Monitor}];export function ThemeSelector(){const{theme,setTheme}=useTheme();const[m,setM]=useState(false);useEffect(()=>setM(true),[]);return <div className="theme-selector" role="group" aria-label="Color theme">{ops.map(({v,I})=><button key={v} aria-label={`Use ${v} theme`} aria-pressed={m&&theme===v} onClick={()=>setTheme(v)}>{m&&theme===v&&<motion.span className="theme-active" layoutId="theme-active"/>}<motion.span whileHover={{rotate:v==="light"?18:0,scale:1.1}}><I/></motion.span></button>)}</div>}