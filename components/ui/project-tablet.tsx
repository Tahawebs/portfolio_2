import Image from "next/image";import {ArrowUpRight} from "lucide-react";import {TiltCard} from "@/components/motion/tilt-card";import type{Project}from "@/config/portfolio";export function ProjectTablet({p,index}:{p:Project;index:number}){return <article className={`project ${index%2?"reverse":""}`}><TiltCard className="tablet"><a href={p.href} target="_blank" rel="noopener noreferrer" aria-label={`Open ${p.name} in a new tab`}><span className="screen"><Image src={p.image} alt={`${p.name} project screenshot`} fill sizes="(max-width:768px) 94vw,70vw"/></span><i aria-hidden/></a></TiltCard><div className="project-meta"><span>{p.technology}</span><h3>{p.name}</h3><ArrowUpRight/></div></article>}