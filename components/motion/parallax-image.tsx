"use client";import {motion,useReducedMotion,useScroll,useTransform} from "framer-motion";import {useRef} from "react";export function ParallaxImage({children}:{children:React.ReactNode}){const ref=useRef(null),r=useReducedMotion(),{scrollYProgress}=useScroll({target:ref}),y=useTransform(scrollYProgress,[0,1],r?[0,0]:[-6,6]);return <motion.div ref={ref} style={{y}}>{children}</motion.div>}