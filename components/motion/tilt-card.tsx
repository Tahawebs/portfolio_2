"use client";import {motion,useMotionValue,useReducedMotion,useSpring,useTransform} from "framer-motion";export function TiltCard({children,className}:{children:React.ReactNode;className?:string}){const r=useReducedMotion(),mx=useMotionValue(.5),my=useMotionValue(.5),rx=useSpring(useTransform(my,[0,1],[3,-3])),ry=useSpring(useTransform(mx,[0,1],[-4,4]));return <motion.div className={className} style={r?{}:{rotateX:rx,rotateY:ry,transformPerspective:1000}} whileTap={{scale:.99}} onPointerMove={e=>{if(r||e.pointerType!=="mouse")return;const b=e.currentTarget.getBoundingClientRect();mx.set((e.clientX-b.left)/b.width);my.set((e.clientY-b.top)/b.height)}} onPointerLeave={()=>{mx.set(.5);my.set(.5)}}>{children}</motion.div>}