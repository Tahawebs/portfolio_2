"use client";import {motion,useMotionValue,useReducedMotion,useSpring} from "framer-motion";export function MagneticButton({children,href,className}:{children:React.ReactNode;href:string;className:string}){const r=useReducedMotion(),x=useSpring(useMotionValue(0)),y=useSpring(useMotionValue(0));return <motion.a className={className} href={href} style={{x,y}} whileTap={{scale:.97}} onPointerMove={e=>{if(r||e.pointerType!=="mouse")return;const b=e.currentTarget.getBoundingClientRect();x.set((e.clientX-b.left-b.width/2)*.12);y.set((e.clientY-b.top-b.height/2)*.12)}} onPointerLeave={()=>{x.set(0);y.set(0)}}>{children}</motion.a>}